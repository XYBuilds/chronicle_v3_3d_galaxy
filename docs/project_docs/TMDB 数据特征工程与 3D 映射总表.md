# **TMDB 数据特征工程与 3D 映射总表**

**架构黄金准则**：

1. **降维克制**：送入 UMAP 的必须是描述电影“文化与内容本质”的绝对核心特征，严禁引入具有多重共线性（Multicollinearity）或高基数（High Cardinality）的噪音数据。  
2. **渲染分离**：描述物理状态（时间、大小、亮度）和具象档案（海报、人名）的数据绝对隔离于算法层，仅由 GPU 和前端 DOM 处理。

### **文本 Embedding 约定（overview + tagline）**

* **目标**：将「剧情语义 + 宣传语调」压入同一条稠密向量，与 genres、original\_language 一并送入 UMAP；**保留所有语言的 overview**，故必须使用**多语言**句向量模型。  
* **阶段 A（轻量化 / subsample 验证）**：`paraphrase-multilingual-MiniLM-L12-v2`，**384 维**；`sentence-transformers` \+ GPU 批编码。  
* **阶段 B（全量 / 周期重构质量版）**：`paraphrase-multilingual-mpnet-base-v2`，**768 维**。  
* **拼接与截断**：`Tagline:` / `Overview:` 前缀两行式拼接；无 tagline 则仅 `Overview:`；整段**从尾部截断**至默认 **3000 字符**；**L2 归一化**后进入后续特征融合与 UMAP。  
* **完整条款**（批大小建议、**PyTorch CUDA / CPU 轮子安装**、`requirements` 锁定、版本语义）：见《TMDB 电影宇宙 Tech Spec》**§2.1.1**。

### **核心映射矩阵规则表**

| 字段名 (Field) | 原数据类型 \-\> 处理后数据类型 | UMAP 算法状态 | 3D 坐标 | 3D 材质 | HUD 2D | 隐藏/逻辑 | 3D 映射与前端渲染策略 | 严谨性标注 / 工程归因 (Rationale) |
| :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- |
| overview | 自由文本 (String) \-\> **稠密文本向量** (Dense Embeddings，**384 或 768 维**，见上文阶段 A/B) | ✅ **核心特征** | ✅ |  | ✅ |  | **决定底层 (X, Y) 坐标**（与 tagline 合并为单条文本后编码，规则见 §文本 Embedding 约定）。在详情抽屉中作为剧情简介文本全量展示。 | **语义锚点**：最核心的文本库，能精准捕获剧本内核，区分“太空歌剧”与“赛博朋克”。多语言保留，禁用纯英文主模型。 |
| tagline | 自由文本 (String) \-\> **同上，与 overview 合并编码** | ✅ **核心特征** | ✅ |  | ✅ |  | **决定底层 (X, Y) 坐标**（合并规则见 §文本 Embedding 约定）。在详情抽屉中作为一句话高亮标语展示。 | **语调锚点**：合并入文本向量能补充影片的核心情绪与宣传定调。 |
| genres | 字符串数组 (Array of Strings) \-\> **递减权重数值向量**（**等比衰减**，默认 \(q=1/\varphi\)，见 Tech Spec §2.1.2） | ✅ **核心特征** | ✅ | ✅ | ✅ |  | **决定底层 (X, Y) 坐标**。并提供双重视觉：宏观星球基色与微观柏林噪声纹理；HUD 悬停时显示文字。 | **拓扑骨架**：摒弃扁平的 One-hot；顺位等比加权（黄金比例公比，可配置）保留主次张力，任意多个标签均有明确权重，防止星团生硬粘连与高阶标签抢权。 |
| original\_language | 离散单值字符串 (String) \-\> **独热稀疏向量** (One-hot) | ✅ **核心特征** | ✅ |  | ✅ |  | **决定底层 (X, Y) 坐标**。不在 3D 渲染表现，但在 HUD 档案抽屉中展示。 | **文化锚点**：干净的文化基底。能促使同流派星云内部自然分化出“好莱坞”与“香港”子星云。 |
| release\_date | 日期字符串 (YYYY-MM-DD) \-\> **绝对小数年份** (Float) | ❌ **绝对排除** | ✅ |  | ✅ |  | **Z 轴 (时空深度)**：平滑映射为历史纵深坐标。同时在 HUD 档案中显示精确文本日期。 | **维度隔离**：时间是独立物理维度，若混入 UMAP 会导致空间结构被强制拉扯成时间轴，破坏语义聚类。 |
| vote\_count | 右偏长尾数值 (Integer) \-\> **对数缩放浮点数** (Log Scaled) | ❌ **绝对排除** |  | ✅ | ✅ |  | **天体体积首选映射**：映射 GPU 粒子半径；HUD 展示具体评价人数。 | **长尾映射**：影视数据呈极度右偏长尾分布。对数缩放可防止头部爆款无限制膨胀遮挡视线。 |
| popularity | 右偏长尾数值 (Float) \-\> **对数缩放浮点数** (Log Scaled) | ❌ **绝对排除** |  | ✅ | ✅ |  | **天体体积备选映射**：可作为体积备选；主要在 HUD 作为热度指数展示。 | **TMDB 内部热度**：反映当前趋势，非历史绝对共识，可作为视觉尺寸的灵活切换项。 |
| imdb\_votes | 右偏长尾数值 (Integer) \-\> **对数缩放浮点数** (Log Scaled) | ❌ **绝对排除** |  | ✅ | ✅ |  | **天体体积备选映射**：可作为体积备选；HUD 展示外部评价基数。 | **外部客观热度**：作为 vote\_count 缺失或偏差时的有效补充和修正参考。 |
| vote\_average | 浮点数 (0.0 \- 10.0) \-\> **保持原值** (Float) | ❌ **绝对排除** |  | ✅ | ✅ |  | **内核发光度首选**：映射 WebGL 材质泛光阈值；HUD 中展示具体评分。 | **反直觉设计**：打破“大星必亮”错觉。建立二维坐标外的独立价值维度（高分刺眼，烂片黯淡）。 |
| imdb\_rating | 浮点数 (0.0 \- 10.0) \-\> **保持原值** (Float) | ❌ **绝对排除** |  | ✅ | ✅ |  | **内核发光度备选**：HUD 中展示外部评分。 | **外部客观评价**：作为防刷分、客观衡量影片口碑的辅助校准指标。 |
| runtime | 整数 (Integer) \-\> **保持原值** (Integer) | ❌ **绝对排除** |  |  | ✅ |  | 仅在 HUD 档案抽屉中作为基础文本属性展示。 | **防噪音**：时长属于纯工业属性，不决定内容的文化相似度，混入算法会产生随机干扰。 |
| production\_countries | 字符串数组 (Array of Strings) \-\> **提取精简数组** (Array) | ❌ **强烈排除** |  |  | ✅ | ✅ | **前端聚光灯 (Spotlight)**：触发国家维度高亮检索；同时在 HUD 中展示。 | **防共线性灾难**：与 original\_language 高度重合，若入算法会使文化权重放大 3 倍，导致宇宙按国家碎裂。 |
| production\_companies | 字符串数组 (Array of Strings) \-\> **提取精简数组** (Array) | ❌ **强烈排除** |  |  | ✅ | ✅ | **前端聚光灯 (Spotlight)**：触发制片厂维度高亮检索；同时在 HUD 中展示。 | **防止商业属性干扰**：厂牌属于工业属性，不足以直接定义影片深层文本语义。 |
| cast | 嵌套对象 (JSON Array) \-\> **提取核心人名列表** (Array) | ❌ **强烈排除** |  |  | ✅ | ✅ | **星座连线交互**：触发主演同盟网；全量加入 HUD 供展示。 | **防维度爆炸**：演员基数极大，若做向量化会导致维度灾难和极度稀疏矩阵，拉爆内存。 |
| director | 嵌套对象/字符串 \-\> **提取核心人名列表** (Array) | ❌ **强烈排除** |  |  | ✅ | ✅ | **星座连线交互**：触发导演宇宙网；全量加入 HUD 供展示。 | **防维度爆炸**：导演基数过大，入算法成本远大于收益，适合做前端寻宝线索。 |
| director\_of\_photography | 嵌套对象/字符串 \-\> **提取核心人名列表** (Array) | ❌ **强烈排除** |  |  | ✅ |  | 仅作为演职员表附属信息在 HUD 抽屉中展示。 | **纯文档特征**：摄影指导对影片主语义影响极度分散，严禁进入聚类引擎。 |
| writers | 嵌套对象/字符串 \-\> **提取核心人名列表** (Array) | ❌ **强烈排除** |  |  | ✅ |  | 仅作为演职员表附属信息在 HUD 抽屉中展示。 | **纯文档特征**：编剧对影片主语义影响极度分散，严禁进入聚类引擎。 |
| producers | 嵌套对象/字符串 \-\> **提取核心人名列表** (Array) | ❌ **强烈排除** |  |  | ✅ |  | 仅作为演职员表附属信息在 HUD 抽屉中展示。 | **纯文档特征**：制片人对影片主语义影响极度分散，严禁进入聚类引擎。 |
| music\_composer | 嵌套对象/字符串 \-\> **提取核心人名列表** (Array) | ❌ **强烈排除** |  |  | ✅ |  | 仅作为演职员表附属信息在 HUD 抽屉中展示。 | **纯文档特征**：配乐对影片主语义影响极度分散，严禁进入聚类引擎。 |
| revenue | 整数 (Integer) \-\> **保持原值或忽略** | ❌ **强烈排除** |  |  | ✅ |  | **HUD 档案补充**：作为票房数据可选展示于档案详情抽屉中。 | **防数据黑洞**：历史数据中有大量 0 值缺失，强行进入 UMAP 会引发算法引力塌陷。 |
| budget | 整数 (Integer) \-\> **保持原值或忽略** | ❌ **强烈排除** |  |  | ✅ |  | **HUD 档案补充**：作为成本数据可选展示于档案详情抽屉中。 | **防数据黑洞**：历史数据中有大量 0 值缺失，强行进入 UMAP 会引发算法引力塌陷。 |
| spoken\_languages | 字符串数组 (Array of Strings) \-\> **提取精简数组** (Array) | ❌ **强烈排除** |  |  | ✅ |  | **HUD 细节彩蛋**：仅在“侧边档案抽屉”底部作为文本标签展示。 | **防特工片悖论**：对白极度杂乱，混入算法会导致多国语言出现的电影在空间中被乱拉扯。 |
| imdb\_id | 字符串 (String) \-\> **保持原值** (String) | ❌ **无需计算** |  |  |  | ✅ | 作为外部链接的拼接基础。 | **外部关联**：不参与特征提取，仅用于跨库查重及前端生成跳转链接。 |
| id | 整数 (Integer) \-\> **主键字符串** (String) | ❌ **无需计算** |  |  |  | ✅ | **射线检测指针**：用于 Raycaster 点击拾取、前后端数据绑定的唯一标识符。 | **去重与关联**：在预处理阶段用作主键合并过滤，绝不参与机器学习。 |
| title | 字符串 (String) \-\> **保持原值** (String) | ❌ **无需计算** |  |  | ✅ |  | **HUD 核心标题**：在悬停 Tooltip 和侧面档案抽屉中展示。 | 纯 UI 渲染层资源，必须完全剥离出 WebGL 层，以保障 GPU 渲染帧率与内存安全。 |
| original\_title | 字符串 (String) \-\> **保持原值** (String) | ❌ **无需计算** |  |  | ✅ |  | **HUD 补充标题**：在侧面档案抽屉中展示。 | 纯 UI 渲染层资源，必须完全剥离出 WebGL 层，以保障 GPU 渲染帧率与内存安全。 |
| poster\_path | 字符串 (String) \-\> **拼装为完整 URL** (String) | ❌ **无需计算** |  |  | ✅ |  | **HUD 视觉主干**：作为海报图片在抽屉中加载展示。 | 纯 UI 渲染层资源，必须完全剥离出 WebGL 层，以保障 GPU 渲染帧率与内存安全。 |
| status | 单一分类字符串 (String) | 🗑️ **直接丢弃** |  |  |  |  | 无。完全废弃。 | **零方差干扰**：绝大多数正式数据均为 Released，毫无区分度，在预处理阶段即刻丢弃。 |


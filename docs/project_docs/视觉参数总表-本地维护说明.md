# 关于 `视觉参数总表.md`（Git 跟踪 · Cursor 不索引）

`docs/project_docs/视觉参数总表.md` **仍在 Git 版本库中**，与常规文档一样提交、合并。

自 **2026-04-27** 起，该路径已列入仓库根目录 **`.cursorignore`**：Cursor 在索引 / 默认上下文时**跳过**此文件，减少大表与扫参备注被自动带入对话的噪音。**需要 AI 引用时**，可 `@` 该文件或临时从 `.cursorignore` 中去掉一行。

## Phase 8 性能基线（独立跟踪）

**[`Phase 8 基线 P8.0 性能与 P8.4 准入.md`](Phase%208%20基线%20P8.0%20性能与%20P8.4%20准入.md)** — Chrome Performance 与 Storybook 双 mesh 准入表（**不在** `.cursorignore` 中，除非另行添加）。

## 从历史提交查看旧版总表

```bash
git log --follow --oneline -- docs/project_docs/视觉参数总表.md
git show <commit>:docs/project_docs/视觉参数总表.md
```

# Chronicle — Python dependencies (default: Windows `.venv` / CPU pipeline).
#
# Canonical version pins live in `requirements.cpu.txt` (included below).
# WSL + GPU (cuML): create conda env from `scripts/env/rapids_env.yml` first;
# optional pip-only extras: `requirements.gpu.txt`.
-r requirements.cpu.txt
